VITE_API_URL=https://figurecollecting.com/api
# Provisional — image-manager's real prod hostname isn't decided yet; the
# runtime localStorage override (resolveImageManagerBaseUrl, matteImageUrl.ts)
# is the escape hatch until this is confirmed.
VITE_IMAGE_MANAGER_URL=https://images.figurecollecting.com
